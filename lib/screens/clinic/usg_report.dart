import 'package:flutter/material.dart';

class USGReport extends StatelessWidget {
  const USGReport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('USG-Clinic'));
  }
}
